"const mongoose = require('mongoose'); const uri = process.env.MONGODB_URI; mongoose.connect(uri).then(() => { console.log('­CONECTADO CON EXITO SERGIO!'); process.exit(0); }).catch(err => { console.error('Error de conexion:', err); process.exit(1); });"  
